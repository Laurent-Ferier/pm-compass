name: Release

# Obsidian requires the release tag to exactly match manifest.json's version
# field with no "v" prefix (see scripts/release.mjs), so this triggers on
# plain semver tags such as "1.0.2".
on:
  push:
    tags:
      - "*.*.*"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 11

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      # The same browser cache CI fills — a release renders no diagrams, but installing
      # puppeteer downloads one all the same. Same key, so a tag reads what master saved.
      - uses: actions/cache@v6
        if: ${{ !env.ACT }}
        with:
          path: ~/.cache/puppeteer
          key: puppeteer-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - run: pnpm install --frozen-lockfile

      - run: pnpm typecheck

      - run: pnpm test

      - run: pnpm lint

      - run: pnpm build

      # vitest runs against src/, so this is the only check that loads the minified bundle
      # that is about to be attested and published.
      - run: pnpm test:bundle

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: "main.js,styles.css"

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            main.js manifest.json styles.css \
            --title "${{ github.ref_name }}" \
            --generate-notes \
          || gh release upload "${{ github.ref_name }}" \
            main.js manifest.json styles.css \
            --clobber
